# Production Environment Configuration for Google Cloud
NODE_ENV=production
PORT=8080

# MongoDB Atlas Connection (Production)
# Note: This is a placeholder. Replace with your actual Wordful MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://wordful-user:${MONGODB_PASSWORD}@wordful-cluster.mongodb.net/wordful-production?retryWrites=true&w=majority
# Keep MONGO_URI for backward compatibility
MONGO_URI=mongodb+srv://wordful-user:${MONGODB_PASSWORD}@wordful-cluster.mongodb.net/wordful-production?retryWrites=true&w=majority

# Security Configuration
# Note: These are placeholders. Replace with your actual secure secrets for Wordful
SESSION_SECRET=wordful-session-secret-for-production
JWT_SECRET=wordful-jwt-secret-for-production
JWT_ACCESS_TOKEN_EXPIRATION=15m
JWT_REFRESH_TOKEN_EXPIRATION=24h

# CORS Configuration for Production
ALLOWED_ORIGINS=https://code-assist-470813.uc.r.appspot.com,https://code-assist-470813.uc.r.appspot.com/,https://wordful.app

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=/var/log/loopjs/app.log

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000
WS_CONNECTION_TIMEOUT=60000