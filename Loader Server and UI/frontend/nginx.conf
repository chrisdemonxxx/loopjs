server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  # API requests will be handled by the frontend app
  # Backend service will be accessed via external Cloud Run URL

  # Handle client-side routing for the React app
  location / {
    try_files $uri $uri/ /index.html;
  }
}